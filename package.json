{
  "name": "echo-tag",
  "version": "1.0.0",
  "description": "Echo Tag: Sonar — a real-time 3D multiplayer hide-and-seek of sound. Move freely; only a hunter's sonar ping or web shot reveals you.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "socket.io-client": "^4.7.5"
  }
}
